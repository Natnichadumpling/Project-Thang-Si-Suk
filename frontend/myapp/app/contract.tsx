import ContractScreen from '../screens/contract'; // ต้องเป็นตัวเล็กตรงกับชื่อไฟล์

export default function ContractPage() {
  return <ContractScreen />;
}

export const options = {
  headerShown: false,
};